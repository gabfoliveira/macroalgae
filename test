library(shiny)

ui <- fluidPage(
  h1("Hello from EDITO"),
  p("This is a minimal Shiny app.")
)

server <- function(input, output, session) {
  # No logic needed for hello world
}

shinyApp(ui = ui, server = server)
